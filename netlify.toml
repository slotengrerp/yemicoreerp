[build]
  command   = "npm run build"
  publish   = "dist"

# SPA redirect — all routes serve index.html so React Router works
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
